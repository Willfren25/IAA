/**
 * Ports (Interfaces/Contratos)
 *
 * Define los contratos que cualquier adaptador debe cumplir.
 * Separa entrada (input), salida (output) y servicios externos.
 * Implementa principio Dependency Inversion (SOLID D)
 */

export {};
